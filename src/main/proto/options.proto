syntax = "proto3";

package demo.options;

option java_package = "com.demo.options"; // For Java we can specify the package name
option java_multiple_files = true; // For Java we can specify to generate multiple files, which means one file per message (aka. No need to access message through outer class)

message AnotherDummy {
    uint32 id = 1;
}
